# 里程碑 M5g 明选引擎与卡片 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地数据驱动明选引擎（触发器/选项/后果/差分）+ 卡片 ①人族噩梦 ②奥丁之祭 ③诺恩三抉择×3 ④菟丝子 ⑤忒修斯之树——五卡全文文本外置于 `choices.json`，改文本不动代码。

**Architecture:** `features/choices/choice_actions.gd`（纯静态逻辑）+ `choice_library.gd`（读 JSON）+ `data/choices.json`（五卡数据）；GameState 新增 5 账本字段（choices_done/truth/drift_extra/race_memory_eff/choice_flags）；RaceManager 人族梦产并入修正系数；DriftActions 叠加 drift_extra；GameManager 明选检测（_pending_choice 占用=停顿点）+ 双信号 + resolve_choice；UI 明选弹层（只监听信号）。

**Tech Stack:** Godot 4.7.1 mono / GDScript / GdUnit4 6.2.1（headless CLI）/ JSON（Godot 4 原生解析）。

**Spec:** `docs/superpowers/specs/2026-09-01-world-tree-m5g-choices-design.md`（权威）；主线 `docs/superpowers/specs/2026-08-31-world-tree-design.md` §13.5（七卡）/§四（明选后果标准）。

## Global Constraints

- 资源一律 BigNum（禁裸 float 存资源）；平衡系数除外
- 逻辑类 RefCounted 纯函数可 headless 测；UI 只通过信号更新，不直改数据（Layer Cake）
- **choices.json 是文本外置核心**：所有卡片全文/数值/flag 都写在 JSON；改文本只许编辑 JSON，禁止把文本硬编码进 .gd（除非是读档兜底文案）
- 中文文件一律用 edit/write 工具或 .NET 显式 UTF-8 读写（禁 PowerShell 默认编码）；**choices.json 用 write 工具写**（UTF-8，杜绝乱码）
- 新增 class_name 脚本后必须先 `godot --headless --path . --import`；`.uid` 确认入库（git status 检查）
- 测试：`godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode --add res://tests/unit/test_xxx.gd -c`；全量去掉 `--add`；退出码 0 = 全绿
- 冒烟：`godot --headless --path . --quit-after 5`（无 SCRIPT ERROR 即通过）
- `is_equal_approx` 双参 `(expected, approx)`；`Array.has()` 非 `contains()`；Array 赋值用 `.assign()`；lambda 按值捕获局部变量（用 Dictionary 包装回写）
- 游戏内文案遵循文风六则（短句呼吸/意象/留白/柔和/自然词汇/人称柔软，spec §11.1）——choices.json 全文已在设计 §4.2 定稿，照抄，不重写
- 存档兼容：5 新字段缺省回退不损坏；旧档（M1-M5f）→ choices_done=[]/truth=0/drift_extra=0.0/race_memory_eff={}/choice_flags=[]
- 每任务一个 commit，风格 `feat: 模块名（要点）`
- **基线测试数：201**（M5f 后全绿）；每任务后全量回归确认无回退

---

### Task 1: GameState 五账本字段 + 序列化回退

**Files:**
- Modify: `features/game/game_state.gd`
- Test: `tests/unit/test_game_state.gd`

**Interfaces:**
- Consumes: 无
- Produces: `choices_done: Array[StringName]`/`truth: int`/`drift_extra: float`/`race_memory_eff: Dictionary`/`choice_flags: Array[StringName]`——全部 to_dict/from_dict + 缺省回退 + 损坏防御

- [ ] **Step 1: 写失败测试**——`tests/unit/test_game_state.gd` 末尾追加 4 用例（仿既有 roundtrip/fallback/corrupt 模式）：

```gdscript
func test_m5g_fields_roundtrip() -> void:
	var s := GameState.new()
	s.choices_done.assign([&"human_nightmare", &"odin_sacrifice"])
	s.truth = 3
	s.drift_extra = 1.5
	s.race_memory_eff["human"] = 0.7
	s.choice_flags.assign([&"odin_name", &"ship_built"])
	var back := GameState.from_dict(s.to_dict())
	assert_that(back.choices_done).contains(&"human_nightmare")
	assert_that(back.truth).is_equal(3)
	assert_that(back.drift_extra).is_equal_approx(1.5, 1e-4)
	assert_that(float(back.race_memory_eff["human"])).is_equal_approx(0.7, 1e-4)
	assert_that(back.choice_flags).contains(&"ship_built")

func test_m5g_fields_missing_fallback() -> void:
	var back := GameState.from_dict({"tick": 5})
	assert_that(back.choices_done).is_empty()
	assert_that(back.truth).is_equal(0)
	assert_that(back.drift_extra).is_equal_approx(0.0, 1e-4)
	assert_that(back.race_memory_eff.is_empty()).is_true()
	assert_that(back.choice_flags).is_empty()

func test_m5g_fields_corrupt_fallback() -> void:
	var back := GameState.from_dict({"truth": "corrupt", "drift_extra": "corrupt", "race_memory_eff": "corrupt"})
	assert_that(back.truth).is_equal(0)
	assert_that(back.drift_extra).is_equal_approx(0.0, 1e-4)
	assert_that(back.race_memory_eff.is_empty()).is_true()

func test_m5g_stringname_arrays_filter_invalid() -> void:
	var back := GameState.from_dict({"choices_done": ["a", 1, {"x": 1}], "choice_flags": ["b", 2.0]})
	assert_that(back.choices_done.size()).is_equal(1)
	assert_that(back.choices_done).contains(&"a")
	assert_that(back.choice_flags.size()).is_equal(1)
	assert_that(back.choice_flags).contains(&"b")
```

- [ ] **Step 2: 跑测试验证失败**

Run: `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode --add res://tests/unit/test_game_state.gd -c`
Expected: FAIL（`Invalid get index 'choices_done'` 等 5 属性不存在错误）

- [ ] **Step 3: 最小实现**——`features/game/game_state.gd` 三处改动（仿既有字段模式，见 §六 决策 4-6）：

① 字段区（`var intimate_events` 之后）：
```gdscript
var choices_done: Array[StringName] = []
var truth: int = 0
var drift_extra: float = 0.0
var race_memory_eff: Dictionary = {}
var choice_flags: Array[StringName] = []
```

② `to_dict()`（`"intimate_events": intimate_events,` 之后）：
```gdscript
        "choices_done": choices_done,
        "truth": truth,
        "drift_extra": drift_extra,
        "race_memory_eff": race_memory_eff,
        "choice_flags": choice_flags,
```

③ `from_dict()`（`s.intimate_events.assign(ie_cleaned)` 之后追加；`s.root_eff_level = ...` 之前即可——注意顺序与 to_dict 无关）：
```gdscript
    var cd: Array = d.get("choices_done", [])
    var cd_cleaned: Array = []
    for x in cd:
        if typeof(x) == TYPE_STRING or typeof(x) == TYPE_STRING_NAME:
            cd_cleaned.append(StringName(x))
    s.choices_done.assign(cd_cleaned)
    var tv: Variant = d.get("truth", 0)
    s.truth = int(tv) if typeof(tv) == TYPE_INT or typeof(tv) == TYPE_FLOAT else 0
    var dv: Variant = d.get("drift_extra", 0.0)
    s.drift_extra = float(dv) if typeof(dv) == TYPE_INT or typeof(dv) == TYPE_FLOAT else 0.0
    var rme: Variant = d.get("race_memory_eff", {})
    if typeof(rme) != TYPE_DICTIONARY:
        rme = {}
    s.race_memory_eff = {}
    for rid2: Variant in rme:
        var rv2: Variant = rme[rid2]
        if typeof(rv2) == TYPE_INT or typeof(rv2) == TYPE_FLOAT:
            s.race_memory_eff[rid2] = float(rv2)
    var cf: Array = d.get("choice_flags", [])
    var cf_cleaned: Array = []
    for x in cf:
        if typeof(x) == TYPE_STRING or typeof(x) == TYPE_STRING_NAME:
            cf_cleaned.append(StringName(x))
    s.choice_flags.assign(cf_cleaned)
```

- [ ] **Step 4: 跑测试验证通过**

Run: 同 Step 2 命令
Expected: PASS（4 新用例绿；既有 20 套件无回归）

- [ ] **Step 5: Commit**

```bash
git add features/game/game_state.gd tests/unit/test_game_state.gd
git commit -m "feat: GameState 扩展（明选五账本：choices_done/truth/drift_extra/race_memory_eff/choice_flags）"
```

---

### Task 2: choices.json 五卡数据 + ChoiceLibrary

**Files:**
- Create: `features/choices/data/choices.json`（★ 五卡全文——设计 §4.2 定稿全文，照抄，一字不改）
- Create: `features/choices/choice_library.gd`
- Test: `tests/unit/test_choice_library.gd`（新文件）

**Interfaces:**
- Consumes: 设计 §4.2 五卡全文（本任务的 JSON 内容源）
- Produces: `ChoiceLibrary.load_all() -> Array[Dictionary]` / `get_choice(id: StringName) -> Dictionary` / `choice_count() -> int` / `valid_choice_ids() -> Array[StringName]`；JSON 结构 `{choices: [{id,title,stage_hint,intro,trigger,options:[{id,text,effects,result_text}]}]}`（option 可带可选 `unlock`：⑤C `{"insight_gte": 8}`——设计 4.2 门槛列的落地位）

- [ ] **Step 1: 创建 choices.json**（write 工具，UTF-8）——七条目（① ② ③past ③now ③future ④ ⑤），全文取自设计 §4.2，数值/flag 照抄：

```json
{
  "choices": [
    {
      "id": "human_nightmare",
      "title": "人族噩梦",
      "stage_hint": "成树·第一次",
      "intro": "夜里，火塘边的人族一起做了同一个梦。\n梦里天裂开，光熄了。\n有人在梦里喊你的名字。",
      "trigger": { "races_awakened": ["human"] },
      "options": [
        {
          "id": "a",
          "text": "把梦收下",
          "effects": {
            "memory": 3.0,
            "truth": 1,
            "relation": { "human": -2 },
            "memory_eff": { "human": 0.7 },
            "flags": ["human_nightmare_harvested"]
          },
          "result_text": "梦很沉。\n醒来的它们眼神发直。\n——没有梦的我们，还算人吗。"
        },
        {
          "id": "b",
          "text": "让它做完",
          "effects": {
            "relation": { "human": 2 },
            "flags": ["human_nightmare_protected"]
          },
          "result_text": "你没有动。\n梦里，人族的孩子追着一道光跑。\n你们围住了那个梦，没有碰它。"
        }
      ]
    },
    {
      "id": "odin_sacrifice",
      "title": "奥丁之祭",
      "stage_hint": "巨树·记忆线",
      "intro": "河底有一段完整的记忆。\n它太重了，要你付出一点什么，才肯上岸。",
      "trigger": { "memory_gte": 30.0 },
      "options": [
        {
          "id": "a",
          "text": "折断一根根须",
          "effects": {
            "growth_pct": -0.30,
            "truth": 2,
            "flags": ["odin_root"]
          },
          "result_text": "断口处，年轮一圈圈翻开。\n第九日的天空，落进你怀里。"
        },
        {
          "id": "b",
          "text": "献出名字",
          "effects": {
            "memory": 6.0,
            "drift": 1.0,
            "flags": ["odin_name"]
          },
          "result_text": "你忽然想不起，自己叫什么。\n树不需要名字。\n……可你刚才，差点想起来。"
        },
        {
          "id": "c",
          "text": "转身离开",
          "effects": {
            "flags": ["odin_left"]
          },
          "result_text": "记忆沉回河底。\n它没有挣扎。\n——就像你曾经见过的那些。"
        }
      ]
    },
    {
      "id": "norne_past",
      "title": "诺恩·过去",
      "stage_hint": "巨树",
      "intro": "记忆里有一扇门。\n门后是旧世界毁灭的那一天。\n推开门，就再关不上了。",
      "trigger": { "memory_gte": 35.0 },
      "options": [
        {
          "id": "a",
          "text": "推开门",
          "effects": {
            "truth": 2,
            "memory_eff": { "human": 0.7 },
            "flags": ["norne_past_revealed"]
          },
          "result_text": "你看见了那一夜。\n火，从天上落下来。\n人族的梦，从此矮了一截。"
        },
        {
          "id": "b",
          "text": "让门关着",
          "effects": {
            "flags": ["norne_past_sealed"]
          },
          "result_text": "天裂那天的事，就让它沉在河底。\n你转过身。门没有动。"
        }
      ]
    },
    {
      "id": "norne_now",
      "title": "诺恩·现在",
      "stage_hint": "巨树",
      "intro": "一个林地民要死了。\n它的歌已经哑了一半。\n你听见它在梦里喊：别让我听完。",
      "trigger": { "memory_gte": 30.0, "races_awakened": ["forestfolk"] },
      "options": [
        {
          "id": "a",
          "text": "用灵魂救它",
          "effects": {
            "soul": { "soul_cost": 2, "revive_pop": { "forestfolk": 3 }, "relation": { "forestfolk": 1 } },
            "flags": ["norne_now_saved"]
          },
          "result_text": "两缕灵魂从河底升起，裹住它。\n它咳出一声歌。\n河，浅了两指。"
        },
        {
          "id": "b",
          "text": "放手",
          "effects": {
            "soul": { "relation": { "forestfolk": -1 } },
            "flags": ["norne_now_let_go"]
          },
          "result_text": "河底多了一缕。\n它很轻。\n你听完了它的歌。"
        }
      ]
    },
    {
      "id": "norne_future",
      "title": "诺恩·未来",
      "stage_hint": "巨树",
      "intro": "石裔说，远方有海。\n海的另一边，也许还有别的土地。\n它们问你：造一艘船，好吗。",
      "trigger": { "memory_gte": 30.0, "races_awakened": ["stoneborn"] },
      "options": [
        {
          "id": "a",
          "text": "让它们造",
          "effects": {
            "flags": ["ship_built"]
          },
          "result_text": "石裔开始丈量天空。\n它们说，船要造得能装下所有歌。"
        },
        {
          "id": "b",
          "text": "不造",
          "effects": {
            "flags": ["ship_refused"]
          },
          "result_text": "锤声停了。\n有人在夜里，指着海的方向。"
        }
      ]
    },
    {
      "id": "dodder",
      "title": "菟丝子",
      "stage_hint": "巨树·夺梦",
      "intro": "你发现自己正在吸林地民的歌。\n不是采。是吸。\n歌顺着你的根，流成了一条河。",
      "trigger": { "plundered_gte": { "forestfolk": 1 } },
      "options": [
        {
          "id": "a",
          "text": "松开",
          "effects": {
            "faith_pct": -0.30,
            "relation": { "forestfolk": 3 },
            "flags": ["dodder_released"]
          },
          "result_text": "萤光重新亮起。\n歌之环，接上了。"
        },
        {
          "id": "b",
          "text": "继续",
          "effects": {
            "memory": 8.0,
            "relation": { "forestfolk": -3 },
            "flags": ["dodder_kept"]
          },
          "result_text": "叶尖挂着半个音。\n风一吹，就散了。\n——只剩风声。"
        }
      ]
    },
    {
      "id": "theseus",
      "title": "忒修斯之树",
      "stage_hint": "巨树·身份",
      "intro": "你换了十七次根须、九次枝干。\n你问自己：还是当初那棵树吗。\n四族围坐着，等你回答。",
      "trigger": { "memory_gte": 30.0 },
      "options": [
        {
          "id": "a",
          "text": "是",
          "effects": {
            "flags": ["theseus_yes"]
          },
          "result_text": "人族学者为此争论了三天。\n（灵魂即身份——它们觉得你答对了，又不甘心。）"
        },
        {
          "id": "b",
          "text": "我不知道",
          "effects": {
            "drift": 1.0,
            "relation": { "human": 1, "forestfolk": 1, "stoneborn": 1, "wildfolk": 1 },
            "flags": ["theseus_uncertain"]
          },
          "result_text": "它们靠得更近了些。\n真实的你，它们认识。"
        },
        {
          "id": "c",
          "text": "不是，但我记得我是",
          "unlock": { "insight_gte": 8 },
          "effects": {
            "insight": 2,
            "truth": 1,
            "flags": ["theseus_remembered"]
          },
          "result_text": "说书人看了你很久。\n她说：'母树，你正在想起自己。'"
        }
      ]
    }
  ]
}
```

> ⚠️ 实现偏差说明：设计 4.2 ①B flag 写 `nightmare_protected`、design 附录写 `human_nightmare_harvested`——本计划统一为 `human_nightmare_protected`/`human_nightmare_harvested`（3.9 命名约定 `<choice_id 前缀>_<语义>` 的落定，附录示例为准）；⑤B 补充 `theseus_uncertain`（设计 4.2 未给 flag，按约定补）。全部 flag 名以本 JSON 为准（测试断言同此）。

- [ ] **Step 2: 写失败测试**——新文件 `tests/unit/test_choice_library.gd`：

```gdscript
extends GdUnitTestSuite

func test_load_all_returns_seven_choices() -> void:
	var choices := ChoiceLibrary.load_all()
	assert_that(choices.size()).is_equal(7)

func test_choice_ids_unique_and_valid() -> void:
	var ids := ChoiceLibrary.valid_choice_ids()
	assert_that(ids.size()).is_equal(7)
	assert_that(ids).contains(&"human_nightmare")
	assert_that(ids).contains(&"odin_sacrifice")
	assert_that(ids).contains(&"norne_past")
	assert_that(ids).contains(&"norne_now")
	assert_that(ids).contains(&"norne_future")
	assert_that(ids).contains(&"dodder")
	assert_that(ids).contains(&"theseus")

func test_choice_count() -> void:
	assert_that(ChoiceLibrary.choice_count()).is_equal(7)

func test_get_choice_returns_struct() -> void:
	var c := ChoiceLibrary.get_choice(&"human_nightmare")
	assert_that(str(c.get("title", ""))).is_equal("人族噩梦")
	assert_that(str(c.get("intro", "")).length()).is_greater(10)
	var opts: Array = c.get("options", [])
	assert_that(opts.size()).is_equal(2)
	assert_that(str(opts[0].get("text", ""))).is_not_empty()

func test_get_choice_unknown_returns_empty() -> void:
	assert_that(ChoiceLibrary.get_choice(&"nope").is_empty()).is_true()

func test_option_texts_follow_style_rules() -> void:
	# 文风六则抽样检查：全文非空、无抽象词堆砌关键词黑名单
	for c in ChoiceLibrary.load_all():
		assert_that(str(c.get("intro", ""))).is_not_empty()
		for opt in c.get("options", []):
			assert_that(str(opt.get("text", ""))).is_not_empty()
			assert_that(str(opt.get("result_text", ""))).is_not_empty()
			var rt := str(opt.get("result_text", ""))
			for banned in ["悲伤", "痛苦", "愤怒", "恐惧", "绝望"]:
				assert_that(rt.contains(banned)).is_false()

func test_theseus_c_option_has_unlock_gate() -> void:
	var c := ChoiceLibrary.get_choice(&"theseus")
	var opts: Array = c.get("options", [])
	var c_opt: Dictionary = opts[2]
	assert_that(int(c_opt.get("unlock", {}).get("insight_gte", 0))).is_equal(8)
```

- [ ] **Step 3: 跑测试验证失败**

Run: `godot --headless --path . --import`（新 class_name 注册），再 `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode --add res://tests/unit/test_choice_library.gd -c`
Expected: FAIL（`Identifier "ChoiceLibrary" not declared` 或解析空）

- [ ] **Step 4: 最小实现**——新文件 `features/choices/choice_library.gd`：

```gdscript
class_name ChoiceLibrary
extends RefCounted

const DATA_PATH := "res://features/choices/data/choices.json"

static var _cache: Array[Dictionary] = []
static var _cache_loaded := false

static func _load_raw() -> Array[Dictionary]:
	if _cache_loaded:
		return _cache
	_cache_loaded = true
	_cache.clear()
	var f := FileAccess.open(DATA_PATH, FileAccess.READ)
	if f == null:
		push_error("无法读取明选数据: %s" % DATA_PATH)
		return _cache
	var text := f.get_as_text()
	f.close()
	var parsed: Variant = JSON.parse_string(text)
	if typeof(parsed) != TYPE_DICTIONARY:
		push_error("明选 JSON 解析失败: %s" % DATA_PATH)
		return _cache
	var list: Variant = parsed.get("choices", [])
	if typeof(list) != TYPE_ARRAY:
		push_error("明选 JSON 缺 choices 数组")
		return _cache
	var seen: Dictionary = {}
	for item: Variant in list:
		if typeof(item) != TYPE_DICTIONARY:
			continue
		var id := StringName(str(item.get("id", "")))
		if id == &"" or seen.has(id):
			continue  # 空 id 或重复 id 跳过
		var opts: Variant = item.get("options", [])
		if typeof(opts) != TYPE_ARRAY or opts.is_empty():
			continue  # 无选项不可玩
		if str(item.get("title", "")).is_empty() or str(item.get("intro", "")).is_empty():
			continue
		seen[id] = true
		_cache.append(item)
	return _cache

static func load_all() -> Array[Dictionary]:
	return _load_raw()

static func get_choice(id: StringName) -> Dictionary:
	for c in _load_raw():
		if StringName(str(c.get("id", ""))) == id:
			return c.duplicate(true)
	return {}

static func choice_count() -> int:
	return _load_raw().size()

static func valid_choice_ids() -> Array[StringName]:
	var out: Array[StringName] = []
	for c in _load_raw():
		out.append(StringName(str(c.get("id", ""))))
	return out
```

- [ ] **Step 5: 跑测试验证通过**

Run: `godot --headless --path . --import` + 同 Step 3 测试命令
Expected: PASS（7 用例全绿）

- [ ] **Step 6: Commit**（含 .uid 入库）

```bash
git add features/choices/ tests/unit/test_choice_library.gd
git status   # 确认 features/choices/choice_library.gd.uid + tests/unit/test_choice_library.gd.uid 已生成
git commit -m "feat: ChoiceLibrary 读 JSON + 五卡数据（choices.json 文本外置，文风六则全文）"
```

---

### Task 3: ChoiceActions 触发解释器 + available

**Files:**
- Create: `features/choices/choice_actions.gd`
- Test: `tests/unit/test_choice_actions.gd`（新文件）

**Interfaces:**
- Consumes: `ChoiceLibrary`（Task 2）、`PlunderActions.reveal_stage/count`、`RelationActions.get_relation`、`SoulActions.RIVER_TOTAL`
- Produces: 静态函数 `_race_awakened(state, id) -> bool` / `_trigger_met(state, trigger) -> bool` / `available(state) -> Array[StringName]` / `first_available(state) -> StringName` / `can_choose(state, choice_id) -> bool` / `option_unlocked(state, choice_id, option_id) -> bool`

- [ ] **Step 1: 写失败测试**——新文件 `tests/unit/test_choice_actions.gd`（本节只测触发，效果在 Task 4）：

```gdscript
extends GdUnitTestSuite

func _awaken(state: GameState, id: StringName) -> void:
	state.races[id] = {"awakened": true, "population": 50.0}

func test_trigger_races_awakened() -> void:
	var s := GameState.new()
	var tr := {"races_awakened": ["human"]}
	assert_that(ChoiceActions._trigger_met(s, tr)).is_false()
	_awaken(s, &"human")
	assert_that(ChoiceActions._trigger_met(s, tr)).is_true()

func test_trigger_memory_gte() -> void:
	var s := GameState.new()
	s.memory = BigNum.new(29.0)
	assert_that(ChoiceActions._trigger_met(s, {"memory_gte": 30.0})).is_false()
	s.memory = BigNum.new(30.0)
	assert_that(ChoiceActions._trigger_met(s, {"memory_gte": 30.0})).is_true()

func test_trigger_faith_growth_insight() -> void:
	var s := GameState.new()
	s.faith = BigNum.new(100.0)
	s.growth = BigNum.new(500.0)
	s.insight = 8
	assert_that(ChoiceActions._trigger_met(s, {"faith_gte": 100.0, "growth_gte": 500.0, "insight_gte": 8})).is_true()
	assert_that(ChoiceActions._trigger_met(s, {"faith_gte": 101.0})).is_false()

func test_trigger_relation_plundered() -> void:
	var s := GameState.new()
	s.relations["human"] = 2
	s.plundered["forestfolk"] = 1
	assert_that(ChoiceActions._trigger_met(s, {"relation_gte": {"human": 2}})).is_true()
	assert_that(ChoiceActions._trigger_met(s, {"relation_gte": {"human": 3}})).is_false()
	assert_that(ChoiceActions._trigger_met(s, {"plundered_gte": {"forestfolk": 1}})).is_true()

func test_trigger_soul_conditions() -> void:
	var s := GameState.new()
	assert_that(ChoiceActions._trigger_met(s, {"soul_revived": true})).is_false()  # river 100 未复活过
	s.soul_river = 99
	assert_that(ChoiceActions._trigger_met(s, {"soul_revived": true})).is_true()
	assert_that(ChoiceActions._trigger_met(s, {"soul_river_lte": 98})).is_false()
	s.soul_river = 88
	assert_that(ChoiceActions._trigger_met(s, {"soul_river_lte": 98})).is_true()

func test_trigger_multiple_all_and() -> void:
	var s := GameState.new()
	_awaken(s, &"forestfolk")
	s.memory = BigNum.new(30.0)
	var tr := {"memory_gte": 30.0, "races_awakened": ["forestfolk"]}
	assert_that(ChoiceActions._trigger_met(s, tr)).is_true()
	s.memory = BigNum.new(29.0)
	assert_that(ChoiceActions._trigger_met(s, tr)).is_false()

func test_available_excludes_done() -> void:
	var s := GameState.new()
	_awaken(s, &"human")
	s.memory = BigNum.new(30.0)
	assert_that(s.choices_done.is_empty()).is_true()
	var avail := ChoiceActions.available(s)
	assert_that(avail.size()).is_equal(3)  # human_nightmare + odin_sacrifice + norne_now? 见下
```

> ⚠️ 注意：`available` 计数依赖 Task 2 JSON 触发条件。基线状态（人族醒+记忆30+林地民未醒+石裔未醒+未夺梦）下触发数组序：human_nightmare(✅人醒)、odin_sacrifice(✅30)、norne_past(❌35)、norne_now(❌林地民未醒)、norne_future(❌石裔未醒)、dodder(❌未夺梦)、theseus(✅30) → **3 个**。若实现与 JSON 不一致该断言失败即取证。

```gdscript
	assert_that(avail).contains(&"human_nightmare")
	assert_that(avail).contains(&"odin_sacrifice")
	assert_that(avail).contains(&"theseus")

func test_first_available_returns_json_order() -> void:
	var s := GameState.new()
	_awaken(s, &"human")
	s.memory = BigNum.new(30.0)
	assert_that(ChoiceActions.first_available(s)).is_equal(&"human_nightmare")  # 数组序第一条

func test_first_available_empty() -> void:
	var s := GameState.new()
	assert_that(ChoiceActions.first_available(s)).is_equal(&"")

func test_can_choose_after_done_false() -> void:
	var s := GameState.new()
	_awaken(s, &"human")
	s.choices_done.assign([&"human_nightmare"])
	assert_that(ChoiceActions.can_choose(s, &"human_nightmare")).is_false()

func test_can_choose_before_trigger_false() -> void:
	var s := GameState.new()  # 人未醒
	assert_that(ChoiceActions.can_choose(s, &"human_nightmare")).is_false()

func test_option_unlocked_no_gate_always_true() -> void:
	var s := GameState.new()
	assert_that(ChoiceActions.option_unlocked(s, &"human_nightmare", &"a")).is_true()

func test_option_unlocked_insight_gate() -> void:
	var s := GameState.new()
	s.insight = 7
	assert_that(ChoiceActions.option_unlocked(s, &"theseus", &"c")).is_false()
	s.insight = 8
	assert_that(ChoiceActions.option_unlocked(s, &"theseus", &"c")).is_true()
```

- [ ] **Step 2: 跑测试验证失败**

Run: `godot --headless --path . --import` + `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode --add res://tests/unit/test_choice_actions.gd -c`
Expected: FAIL（`Identifier "ChoiceActions" not declared`）

- [ ] **Step 3: 最小实现**——新文件 `features/choices/choice_actions.gd`：

```gdscript
class_name ChoiceActions
extends RefCounted

static func _race_awakened(state: GameState, race_id: StringName) -> bool:
	return state.races.has(race_id) and bool(state.races[race_id].get("awakened", false))

static func _trigger_met(state: GameState, trigger: Dictionary) -> bool:
	if trigger.is_empty():
		return true
	if trigger.has("races_awakened"):
		var list: Array = trigger["races_awakened"]
		for rid in list:
			if not _race_awakened(state, StringName(str(rid))):
				return false
	if trigger.has("memory_gte"):
		if not state.memory.is_greater_or_equal(BigNum.new(float(trigger["memory_gte"]))):
			return false
	if trigger.has("faith_gte"):
		if not state.faith.is_greater_or_equal(BigNum.new(float(trigger["faith_gte"]))):
			return false
	if trigger.has("growth_gte"):
		if not state.growth.is_greater_or_equal(BigNum.new(float(trigger["growth_gte"]))):
			return false
	if trigger.has("insight_gte"):
		if state.insight < int(trigger["insight_gte"]):
			return false
	if trigger.has("relation_gte"):
		for rid2 in trigger["relation_gte"]:
			var need := int(trigger["relation_gte"][rid2])
			if RelationActions.get_relation(state, StringName(str(rid2))) < need:
				return false
	if trigger.has("plundered_gte"):
		for rid3 in trigger["plundered_gte"]:
			var need2 := int(trigger["plundered_gte"][rid3])
			if PlunderActions.count(state, StringName(str(rid3))) < need2:
				return false
	if trigger.has("soul_revived") and bool(trigger["soul_revived"]):
		if state.soul_river >= SoulActions.RIVER_TOTAL:
			return false  # 河底满 = 从未复活（复活必 -1）
	if trigger.has("soul_river_lte"):
		if state.soul_river > int(trigger["soul_river_lte"]):
			return false
	return true

static func available(state: GameState) -> Array[StringName]:
	var out: Array[StringName] = []
	for c in ChoiceLibrary.load_all():
		var id := StringName(str(c.get("id", "")))
		if state.choices_done.has(id):
			continue
		if _trigger_met(state, c.get("trigger", {})):
			out.append(id)
	return out

static func first_available(state: GameState) -> StringName:
	var list := available(state)
	if list.is_empty():
		return &""
	return list[0]

static func can_choose(state: GameState, choice_id: StringName) -> bool:
	if state.choices_done.has(choice_id):
		return false
	var c := ChoiceLibrary.get_choice(choice_id)
	if c.is_empty():
		return false
	return _trigger_met(state, c.get("trigger", {}))

static func option_unlocked(state: GameState, choice_id: StringName, option_id: StringName) -> bool:
	var c := ChoiceLibrary.get_choice(choice_id)
	if c.is_empty():
		return false
	for opt: Variant in c.get("options", []):
		if typeof(opt) != TYPE_DICTIONARY:
			continue
		if StringName(str(opt.get("id", ""))) != option_id:
			continue
		var unlock: Variant = opt.get("unlock", {})
		if typeof(unlock) != TYPE_DICTIONARY:
			return true
		return _trigger_met(state, unlock)
	return false
```

- [ ] **Step 4: 跑测试验证通过**

Run: `godot --headless --path . --import` + 同 Step 2 测试命令
Expected: PASS（11 用例全绿——若 `test_available_excludes_done` 计数≠3 即为 JSON/实现不一致，取证后按设计 §4.1 修正断言或 JSON）

- [ ] **Step 5: Commit**

```bash
git add features/choices/choice_actions.gd tests/unit/test_choice_actions.gd
git status   # .uid 确认
git commit -m "feat: ChoiceActions 触发解释器（条件键全支持/available 数组序/选项门槛）"
```

---

### Task 4: ChoiceActions 后果执行（resolve + effects 全键）

**Files:**
- Modify: `features/choices/choice_actions.gd`
- Test: `tests/unit/test_choice_actions.gd`（追加用例）

**Interfaces:**
- Consumes: Task 3 的 `can_choose`/`option_unlocked`
- Produces: `resolve(state, choice_id, option_id) -> Dictionary`——应用全部 effects 键后返回 `{"ok": true, "id": choice_id, "option": option_id, "result_text": str, "effects": {...}}`；失败 `{"ok": false}`；成功后 `choices_done` 记入该 choice_id

- [ ] **Step 1: 写失败测试**——`tests/unit/test_choice_actions.gd` 末尾追加（覆盖每个 effects 键）：

```gdscript
# ---------- 后果执行 ----------

func _ready_choice(state: GameState) -> void:
	_awaken(state, &"human")
	_awaken(state, &"forestfolk")
	state.memory = BigNum.new(40.0)
	state.faith = BigNum.new(200.0)
	state.growth = BigNum.new(1000.0)
	state.soul_river = 98

func test_resolve_human_nightmare_a() -> void:
	var s := GameState.new()
	_awaken(s, &"human")
	# A 采梦：memory+3 truth+1 relation-2 memory_eff 0.7
	var r := ChoiceActions.resolve(s, &"human_nightmare", &"a")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.memory.to_value()).is_equal_approx(3.0, 1e-4)
	assert_that(s.truth).is_equal(1)
	assert_that(int(s.relations["human"])).is_equal(-2)
	assert_that(float(s.race_memory_eff["human"])).is_equal_approx(0.7, 1e-4)
	assert_that(s.choices_done).contains(&"human_nightmare")
	assert_that(str(r.get("result_text", "")).length()).is_greater(5)

func test_resolve_marks_done_and_blocks_rechoose() -> void:
	var s := GameState.new()
	_awaken(s, &"human")
	assert_that(ChoiceActions.resolve(s, &"human_nightmare", &"b").get("ok", false)).is_true()
	# 已 done：再次 resolve 失败
	assert_that(ChoiceActions.resolve(s, &"human_nightmare", &"a").get("ok", false)).is_false()

func test_resolve_odin_b_drift_and_memory() -> void:
	var s := GameState.new()
	s.memory = BigNum.new(30.0)
	var r := ChoiceActions.resolve(s, &"odin_sacrifice", &"b")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.memory.to_value()).is_equal_approx(36.0, 1e-4)
	assert_that(s.drift_extra).is_equal_approx(1.0, 1e-4)
	assert_that(s.choice_flags).contains(&"odin_name")

func test_resolve_odin_a_growth_pct() -> void:
	var s := GameState.new()
	s.memory = BigNum.new(30.0)
	s.growth = BigNum.new(1000.0)
	var r := ChoiceActions.resolve(s, &"odin_sacrifice", &"a")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.growth.to_value()).is_equal_approx(700.0, 1e-4)  # -30%
	assert_that(s.truth).is_equal(2)

func test_resolve_norne_now_a_soul_operation() -> void:
	var s := GameState.new()
	_awaken(s, &"forestfolk")
	s.memory = BigNum.new(40.0)
	s.soul_river = 98
	var r := ChoiceActions.resolve(s, &"norne_now", &"a")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.soul_river).is_equal(96)  # -2 缕
	assert_that(float(s.races["forestfolk"]["population"])).is_equal_approx(53.0, 1e-4)  # 50+3
	assert_that(int(s.relations["forestfolk"])).is_equal(1)
	assert_that(s.choice_flags).contains(&"norne_now_saved")

func test_resolve_norne_now_b_let_go() -> void:
	var s := GameState.new()
	_awaken(s, &"forestfolk")
	s.memory = BigNum.new(40.0)
	var r := ChoiceActions.resolve(s, &"norne_now", &"b")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.soul_river).is_equal(100)  # 无变化
	assert_that(int(s.relations["forestfolk"])).is_equal(-1)
	assert_that(s.choice_flags).contains(&"norne_now_let_go")

func test_resolve_dodder_a_faith_pct_relation() -> void:
	var s := GameState.new()
	s.plundered["forestfolk"] = 1
	s.faith = BigNum.new(200.0)
	var r := ChoiceActions.resolve(s, &"dodder", &"a")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.faith.to_value()).is_equal_approx(140.0, 1e-4)  # -30%
	assert_that(int(s.relations["forestfolk"])).is_equal(3)
	assert_that(s.choice_flags).contains(&"dodder_released")

func test_resolve_theseus_b_four_race_relations() -> void:
	var s := GameState.new()
	s.memory = BigNum.new(30.0)
	var r := ChoiceActions.resolve(s, &"theseus", &"b")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.drift_extra).is_equal_approx(1.0, 1e-4)
	for rid in [&"human", &"forestfolk", &"stoneborn", &"wildfolk"]:
		assert_that(int(s.relations[rid])).is_equal(1)
	assert_that(s.choice_flags).contains(&"theseus_uncertain")

func test_resolve_theseus_c_requires_unlock() -> void:
	var s := GameState.new()
	s.memory = BigNum.new(30.0)
	s.insight = 7
	assert_that(ChoiceActions.resolve(s, &"theseus", &"c").get("ok", false)).is_false()  # 门槛不够
	s.insight = 8
	var r := ChoiceActions.resolve(s, &"theseus", &"c")
	assert_that(r.get("ok", false)).is_true()
	assert_that(s.insight).is_equal(10)  # +2
	assert_that(s.truth).is_equal(1)
	assert_that(s.choice_flags).contains(&"theseus_remembered")

func test_resolve_before_trigger_fails() -> void:
	var s := GameState.new()  # 人未醒
	assert_that(ChoiceActions.resolve(s, &"human_nightmare", &"a").get("ok", false)).is_false()
	assert_that(s.choices_done.is_empty()).is_true()
```

- [ ] **Step 2: 跑测试验证失败**

Run: 同 Task 3 Step 2 命令（--add test_choice_actions.gd -c）
Expected: FAIL（`Nonexistent function 'resolve'`）

- [ ] **Step 3: 最小实现**——`features/choices/choice_actions.gd` 末尾追加（含私有效果应用函数）：

```gdscript
static func _apply_effects(state: GameState, effects: Dictionary) -> void:
	if effects.has("memory"):
		state.memory.add(BigNum.new(float(effects["memory"])))
	if effects.has("faith"):
		state.faith.add(BigNum.new(float(effects["faith"])))
	if effects.has("growth_pct"):
		var p := float(effects["growth_pct"])
		state.growth.add(BigNum.new(state.growth.to_value() * p))
	if effects.has("faith_pct"):
		var p2 := float(effects["faith_pct"])
		state.faith.add(BigNum.new(state.faith.to_value() * p2))
	if effects.has("relation"):
		for rid in effects["relation"]:
			RelationActions.apply_change(state, StringName(str(rid)), int(effects["relation"][rid]))
	if effects.has("insight"):
		state.insight += int(effects["insight"])
	if effects.has("truth"):
		state.truth += int(effects["truth"])
	if effects.has("drift"):
		state.drift_extra += float(effects["drift"])
	if effects.has("memory_eff"):
		for rid2 in effects["memory_eff"]:
			state.race_memory_eff[rid2] = float(effects["memory_eff"][rid2])
	if effects.has("soul"):
		var soul_op: Dictionary = effects["soul"]
		if soul_op.has("soul_cost"):
			state.soul_river = maxi(state.soul_river - int(soul_op["soul_cost"]), 0)
		if soul_op.has("revive_pop"):
			for rid3 in soul_op["revive_pop"]:
				if not state.races.has(rid3):
					continue
				state.races[rid3]["population"] = float(state.races[rid3].get("population", 0.0)) + float(soul_op["revive_pop"][rid3])
		if soul_op.has("relation"):
			for rid4 in soul_op["relation"]:
				RelationActions.apply_change(state, StringName(str(rid4)), int(soul_op["relation"][rid4]))
	if effects.has("flags"):
		for f in effects["flags"]:
			var fn := StringName(str(f))
			if not state.choice_flags.has(fn):
				state.choice_flags.append(fn)

static func resolve(state: GameState, choice_id: StringName, option_id: StringName) -> Dictionary:
	if not can_choose(state, choice_id):
		return {"ok": false}
	var c := ChoiceLibrary.get_choice(choice_id)
	for opt: Variant in c.get("options", []):
		if typeof(opt) != TYPE_DICTIONARY:
			continue
		if StringName(str(opt.get("id", ""))) != option_id:
			continue
		if not option_unlocked(state, choice_id, option_id):
			return {"ok": false}
		_apply_effects(state, opt.get("effects", {}))
		state.choices_done.append(choice_id)
		return {"ok": true, "id": choice_id, "option": option_id,
			"result_text": str(opt.get("result_text", "")), "effects": opt.get("effects", {})}
	return {"ok": false}
```

- [ ] **Step 4: 跑测试验证通过**

Run: 同 Step 2 命令
Expected: PASS（10 新用例绿；本节累计 21 用例）

- [ ] **Step 5: Commit**

```bash
git add features/choices/choice_actions.gd tests/unit/test_choice_actions.gd
git commit -m "feat: ChoiceActions 后果执行（effects 全键：资源/关系/领悟/真相/漂移/梦产/灵魂/flag）"
```

---

### Task 5: RaceManager 梦产系数 + DriftActions 漂移叠加

**Files:**
- Modify: `features/races/race_manager.gd`（第 4 步人族/记忆产出）
- Modify: `features/memories/drift_actions.gd`（drift_value）
- Test: `tests/unit/test_race_manager.gd`、`tests/unit/test_drift_actions.gd`（追加）

**Interfaces:**
- Consumes: Task 1 的 `race_memory_eff` / `drift_extra`
- Produces: RaceManager 记忆产出 × 系数（缺省 1.0）；DriftActions.drift_value 叠加 drift_extra 后 clamp

- [ ] **Step 1: 写失败测试**——`tests/unit/test_race_manager.gd` 追加 1 用例、`tests/unit/test_drift_actions.gd` 追加 2 用例：

```gdscript
# test_race_manager.gd 末尾——race_memory_eff 并入人族梦产
func test_memory_eff_multiplier_applied() -> void:
	var s := GameState.new()
	s.races["human"] = {"awakened": true, "population": 50.0}
	s.sap = BigNum.new(500.0)
	s.race_memory_eff["human"] = 0.7  # ①A 人族失眠
	RaceManager.tick_races(s)
	# 原公式 50×0.001×1.0（root_eff 0）= 0.05 → ×0.7 = 0.035
	assert_that(s.memory.to_value()).is_equal_approx(0.035, 1e-4)

func test_memory_eff_default_no_change() -> void:
	var s := GameState.new()
	s.races["human"] = {"awakened": true, "population": 50.0}
	s.sap = BigNum.new(500.0)
	RaceManager.tick_races(s)
	assert_that(s.memory.to_value()).is_equal_approx(0.05, 1e-4)  # 50×0.001
```

```gdscript
# test_drift_actions.gd 末尾——drift_extra 叠加
func test_drift_extra_adds_to_pure_state() -> void:
	var s := GameState.new()
	s.drift_extra = 1.0
	assert_that(DriftActions.drift_value(s)).is_equal_approx(1.0, 1e-4)
	assert_that(DriftActions.drift_tier(s)).is_equal(0)  # <3

func test_drift_extra_pushes_tier() -> void:
	var s := GameState.new()
	s.drift_extra = 4.0
	assert_that(DriftActions.drift_value(s)).is_equal_approx(4.0, 1e-4)
	assert_that(DriftActions.drift_tier(s)).is_equal(1)  # ≥3

func test_drift_extra_clamped_at_max() -> void:
	var s := GameState.new()
	s.plundered["human"] = 20  # 20×0.5 = 10（已满）
	s.drift_extra = 3.0
	assert_that(DriftActions.drift_value(s)).is_equal_approx(10.0, 1e-4)  # clamp 不溢出
```

- [ ] **Step 2: 跑测试验证失败**

Run: `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode --add res://tests/unit/test_race_manager.gd -c` 与 `... --add res://tests/unit/test_drift_actions.gd -c`
Expected: FAIL（test_drift_extra_* 失败：drift_value 未含 extra；test_memory_eff_multiplier_fails：产出=0.05 非 0.035）

- [ ] **Step 3: 最小实现**——两文件各一处：

`features/races/race_manager.gd` 第 4 步（`if race.produce_memory:` 行）：
```gdscript
		if race.produce_memory:
			var mem_eff := float(state.race_memory_eff.get(race.id, 1.0))
			state.memory.add(BigNum.new(pop * MEMORY_EFF * mem_eff * (1.0 + 0.1 * float(state.root_eff_level))))
```

`features/memories/drift_actions.gd`（drift_value 的 return 行）：
```gdscript
	return clampf(float(total_plundered) * PLUNDER_DRIFT + memory_drift + state.drift_extra, 0.0, DRIFT_MAX)
```

- [ ] **Step 4: 跑测试验证通过**

Run: 同 Step 2 两命令
Expected: PASS（两文件新用例全绿；既有无回归）

- [ ] **Step 5: Commit**

```bash
git add features/races/race_manager.gd features/memories/drift_actions.gd tests/unit/test_race_manager.gd tests/unit/test_drift_actions.gd
git commit -m "feat: 明选后果接入（人族梦产×系数/漂移叠加 drift_extra）"
```

---

### Task 6: GameManager 集成（明选检测 + 信号 + resolve 入口）

**Files:**
- Modify: `autoloads/game_manager.gd`
- Test: `tests/unit/test_game_manager.gd`（追加）

**Interfaces:**
- Consumes: Task 2-4 的 ChoiceLibrary/ChoiceActions
- Produces: 信号 `choice_available(choice_id: StringName, title: String, intro: String, options: Array)` / `choice_resolved(choice_id: StringName, option_id: StringName, result_text: String, option_text: String)`；成员 `_pending_choice: StringName`；方法 `resolve_choice(choice_id, option_id) -> Dictionary`；`_process` 末尾明选检测（tick 后、resources_changed 前）

- [ ] **Step 1: 写失败测试**——`tests/unit/test_game_manager.gd` 追加（仿 intimate/plunder 入口测试模式 + pending 语义）：

```gdscript
func test_choice_available_emitted_on_tick() -> void:
	gm._state = GameState.new()
	gm._state.races["human"] = {"awakened": true, "population": 50.0}
	var got := {"ok": false, "title": ""}
	gm.choice_available.connect(func(id: StringName, title: String, intro: String, options: Array) -> void:
		got["ok"] = true
		got["title"] = title)
	gm._process(1.0)  # tick 触发人族召唤 → 明选检测
	assert_that(got["ok"]).is_true()
	assert_that(str(got["title"])).is_equal("人族噩梦")

func test_choice_pending_blocks_second_emit() -> void:
	# 一次只弹一个：pending 未清前不重复发
	gm._state = GameState.new()
	gm._state.races["human"] = {"awakened": true, "population": 50.0}
	gm._state.memory = BigNum.new(30.0)  # 3 个 available，但只弹第一个
	var count := 0
	gm.choice_available.connect(func(id: StringName, title: String, intro: String, options: Array) -> void:
		count += 1)
	gm._process(1.0)
	gm._process(1.0)
	assert_that(count).is_equal(1)
	assert_that(gm._pending_choice).is_equal(&"human_nightmare")

func test_resolve_choice_signal_and_effect() -> void:
	gm._state = GameState.new()
	gm._state.races["human"] = {"awakened": true, "population": 50.0}
	var got := {"ok": false, "text": ""}
	gm.choice_resolved.connect(func(id: StringName, opt: StringName, text: String, opt_text: String) -> void:
		got["ok"] = true
		got["text"] = text)
	gm._process(1.0)  # 弹 human_nightmare
	var r: Dictionary = gm.resolve_choice(&"human_nightmare", &"a")
	assert_that(r.get("ok", false)).is_true()
	assert_that(got["ok"]).is_true()
	assert_that(str(got["text"]).length()).is_greater(10)
	assert_that(gm.get_state().choices_done).contains(&"human_nightmare")
	assert_that(gm._pending_choice).is_equal(&"")

func test_resolve_choice_wrong_pending_fails() -> void:
	gm._state = GameState.new()
	gm._state.races["human"] = {"awakened": true, "population": 50.0}
	gm._process(1.0)
	# 尝试解一个非 pending 的明选（未触发）
	var r: Dictionary = gm.resolve_choice(&"theseus", &"a")
	assert_that(r.get("ok", false)).is_false()
	assert_that(gm._pending_choice).is_equal(&"human_nightmare")  # pending 不被误清

func test_resolve_choice_no_pending_fails() -> void:
	gm._state = GameState.new()  # 无触发
	var r: Dictionary = gm.resolve_choice(&"human_nightmare", &"a")
	assert_that(r.get("ok", false)).is_false()
```

- [ ] **Step 2: 跑测试验证失败**

Run: `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode --add res://tests/unit/test_game_manager.gd -c`
Expected: FAIL（`Nonexistent signal 'choice_available'` / `Nonexistent function 'resolve_choice'` / `_pending_choice` 未声明）

- [ ] **Step 3: 最小实现**——`autoloads/game_manager.gd` 三处改动：

① 信号区（`signal intimate_done` 后）：
```gdscript
signal choice_available(choice_id: StringName, title: String, intro: String, options: Array)
signal choice_resolved(choice_id: StringName, option_id: StringName, result_text: String, option_text: String)
```

② 成员区（`var _tick_accumulator := 0.0` 后）：
```gdscript
var _pending_choice: StringName = &""
```

③ `_process` tick 块内（`resources_changed.emit()` 之前）：
```gdscript
		_check_choice_trigger()
```

④ 新方法（文件末尾）：
```gdscript
func _check_choice_trigger() -> void:
	if _pending_choice != &"":
		return
	var cid := ChoiceActions.first_available(_state)
	if cid == &"":
		return
	_pending_choice = cid
	var c := ChoiceLibrary.get_choice(cid)
	choice_available.emit(cid, str(c.get("title", "")), str(c.get("intro", "")), c.get("options", []))

func resolve_choice(choice_id: StringName, option_id: StringName) -> Dictionary:
	if _pending_choice != choice_id:
		return {"ok": false}
	var result := ChoiceActions.resolve(_state, choice_id, option_id)
	if not result.get("ok", false):
		return result
	_pending_choice = &""
	# 第 4 参 = 所选选项的按钮文字（从 JSON options 按 option_id 取 text）
	var c := ChoiceLibrary.get_choice(choice_id)
	var opt_text := ""
	for opt: Variant in c.get("options", []):
		if typeof(opt) == TYPE_DICTIONARY and StringName(str(opt.get("id", ""))) == option_id:
			opt_text = str(opt.get("text", ""))
			break
	choice_resolved.emit(choice_id, option_id, str(result.get("result_text", "")), opt_text)
	resources_changed.emit()
	return result
```

> ⚠️ 语义说明：`choice_resolved` 第 4 参 `option_text` = 所选选项的**按钮文字**（如「把梦收下」），供 UI 播报「（明选·把梦收下）」——设计 3.7 信号签名注释含糊，此为实现裁决：取选项 text 非 title。若裁决错误，代价：UI 差分播报显示选项文字而非标题（无害）。

- [ ] **Step 4: 跑测试验证通过**

Run: 同 Step 2 命令
Expected: PASS（5 新用例全绿——若 `test_choice_pending_blocks_second_emit` 断言 `_pending_choice` 私有字段访问报错，改为断言 `count == 1` + 二次 resolve 失败即可，GDScript 无真私有，直接可读）

- [ ] **Step 5: Commit**

```bash
git add autoloads/game_manager.gd tests/unit/test_game_manager.gd
git commit -m "feat: GameManager 明选集成（_pending_choice 停顿点/双信号/resolve_choice）"
```

---

### Task 7: UI 明选弹层

**Files:**
- Modify: `features/ui/main.tscn`
- Modify: `features/ui/main.gd`
- Test: headless 冒烟 + 全量回归（UI 无单测，按项目惯例）

**Interfaces:**
- Consumes: `GameManager.choice_available/choice_resolved`（Task 6）、`ChoiceActions.option_unlocked`（选项灰显）
- Produces: `%ChoicePanel`（隐藏默认）+ `%ChoiceTitleLabel` + `%ChoiceIntroLabel` + `%ChoiceOptionA/B/CButton` + `_refresh_choice()` / `_on_choice_available` / `_on_choice_pressed(option_id)` / `_on_choice_resolved`；读档恢复重发

- [ ] **Step 1: 改 main.tscn**——VBox 内末尾（`PlunderSoulWildButton` 之后）追加弹层（仿 DreamPanel/TotemPanel）：

```text
[node name="ChoicePanel" type="PanelContainer" parent="VBox"]
unique_name_in_owner = true
visible = false
layout_mode = 2

[node name="ChoiceVBox" type="VBoxContainer" parent="VBox/ChoicePanel"]
layout_mode = 2

[node name="ChoiceTitleLabel" type="Label" parent="VBox/ChoicePanel/ChoiceVBox"]
unique_name_in_owner = true
layout_mode = 2
text = ""
autowrap_mode = 3

[node name="ChoiceIntroLabel" type="Label" parent="VBox/ChoicePanel/ChoiceVBox"]
unique_name_in_owner = true
layout_mode = 2
text = ""
autowrap_mode = 3

[node name="ChoiceOptionAButton" type="Button" parent="VBox/ChoicePanel/ChoiceVBox"]
unique_name_in_owner = true
layout_mode = 2
text = ""

[node name="ChoiceOptionBButton" type="Button" parent="VBox/ChoicePanel/ChoiceVBox"]
unique_name_in_owner = true
layout_mode = 2
text = ""

[node name="ChoiceOptionCButton" type="Button" parent="VBox/ChoicePanel/ChoiceVBox"]
unique_name_in_owner = true
layout_mode = 2
text = ""
```

- [ ] **Step 2: 改 main.gd**——四处改动：

① `@onready` 区（`plunder_soul_wild_button` 后）：
```gdscript
@onready var choice_panel: PanelContainer = %ChoicePanel
@onready var choice_title_label: Label = %ChoiceTitleLabel
@onready var choice_intro_label: Label = %ChoiceIntroLabel
@onready var choice_option_a_button: Button = %ChoiceOptionAButton
@onready var choice_option_b_button: Button = %ChoiceOptionBButton
@onready var choice_option_c_button: Button = %ChoiceOptionCButton
```

② `_ready()` 连接区（`GameManager.intimate_done.connect` 后）：
```gdscript
	choice_option_a_button.pressed.connect(func(): _on_choice_pressed(&"a"))
	choice_option_b_button.pressed.connect(func(): _on_choice_pressed(&"b"))
	choice_option_c_button.pressed.connect(func(): _on_choice_pressed(&"c"))
	GameManager.choice_available.connect(_on_choice_available)
	GameManager.choice_resolved.connect(_on_choice_resolved)
```

③ 新方法（文件末尾）：
```gdscript
func _on_choice_available(choice_id: StringName, title: String, intro: String, options: Array) -> void:
	choice_title_label.text = title
	choice_intro_label.text = intro
	var s := GameManager.get_state()
	var buttons := [choice_option_a_button, choice_option_b_button, choice_option_c_button]
	for i in mini(options.size(), buttons.size()):
		var opt: Variant = options[i]
		if typeof(opt) != TYPE_DICTIONARY:
			buttons[i].visible = false
			continue
		var opt_id := StringName(str(opt.get("id", "")))
		buttons[i].visible = true
		buttons[i].text = str(opt.get("text", ""))
		buttons[i].disabled = not ChoiceActions.option_unlocked(s, choice_id, opt_id)
	for i in range(options.size(), buttons.size()):
		buttons[i].visible = false
	choice_panel.visible = true

func _on_choice_pressed(option_id: StringName) -> void:
	# 当前弹层的 choice_id 由 GameManager._pending_choice 持有，经 resolve_choice 校验
	var s := GameManager.get_state()
	var cid := GameManager._pending_choice
	var result: Dictionary = GameManager.resolve_choice(cid, option_id)
	# 成功显示由 _on_choice_resolved 处理；失败静默（门槛拦截已在按钮 disabled 挡住）

func _on_choice_resolved(choice_id: StringName, option_id: StringName, result_text: String, option_text: String) -> void:
	race_event_label.text = result_text
	log_label.text = "（明选·%s）" % option_text
	choice_panel.visible = false
	_refresh()
```

④ `_ready()` 兜底（`_refresh()` 调用前，读档恢复——若 _pending_choice 非空重发弹层）：
```gdscript
	if GameManager._pending_choice != &"":
		var c := ChoiceLibrary.get_choice(GameManager._pending_choice)
		if not c.is_empty():
			_on_choice_available(GameManager._pending_choice, str(c.get("title", "")), str(c.get("intro", "")), c.get("options", []))
```

> ⚠️ 读档恢复的机制说明：`_pending_choice` 非存档字段（运行态）——存档退出时 pending 丢失，读档后 `first_available` 会重新触发（choices_done 未记）。故此兜底实际只在**同一进程内场景重载**时生效；真正的读档恢复靠 Task 6 的 `_check_choice_trigger` 在第一个 tick 自然重发。此兜底保留（对齐人族唤醒读档播报模式），无副作用。

- [ ] **Step 3: 冒烟 + 全量回归**

Run: `godot --headless --path . --quit-after 5` → 无 SCRIPT ERROR；全量 `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode`
Expected: 冒烟 0、全量全绿

- [ ] **Step 4: Commit**

```bash
git add features/ui/main.tscn features/ui/main.gd
git commit -m "feat: UI 明选弹层（intro/选项按钮/门槛灰显/差分播报）"
```

---

### Task 8: 临时 E2E 全链路 + 文档同步

**Files:**
- Create: `tests/e2e_m5g.gd`（临时，跑完删）
- Modify: `docs/world-tree/CONTINUE.md`、`docs/world-tree/ROADMAP.md`

**Interfaces:**
- Consumes: GameManager 全部明选入口 + 五卡数据

- [ ] **Step 1: 写 E2E 脚本**——`tests/e2e_m5g.gd`（extends SceneTree，仿 M5f E2E 模式；`load(...).new()` 返回值无静态类型 → 无类型 var + 显式 Dictionary，M2 教训 #4）：

```gdscript
extends SceneTree

var _checks := 0
var _fails := 0

func _check(name: String, cond: bool) -> void:
	_checks += 1
	if cond:
		print("PASS  ", name)
	else:
		_fails += 1
		print("FAIL  ", name)

func _init() -> void:
	var gm = load("res://autoloads/game_manager.gd").new()
	gm._state = GameState.new()
	# 1) 人族唤醒 → 第一次明选弹出
	gm._state.races["human"] = {"awakened": true, "population": 50.0}
	gm._process(1.0)
	_check("人族噩梦弹出", gm._pending_choice == &"human_nightmare")
	# 人族每 tick 产 50×0.001=0.05 记忆（produce_memory=true）——资源断言一律用 resolve 前后差值
	var m0 := gm.get_state().memory.to_value()
	var r: Dictionary = gm.resolve_choice(&"human_nightmare", &"a")
	_check("采梦成功", bool(r.get("ok", false)))
	_check("采梦记忆+3", is_equal_approx(gm.get_state().memory.to_value() - m0, 3.0))
	_check("真相+1", gm.get_state().truth == 1)
	_check("人族关系-2", RelationActions.get_relation(gm.get_state(), &"human") == -2)
	_check("人族梦产×0.7", float(gm.get_state().race_memory_eff["human"]) == 0.7)
	# 2) 记忆 30 → 奥丁之祭（B 献名字漂移）
	var s := gm.get_state()
	s.memory = BigNum.new(30.0)
	gm._process(1.0)
	_check("奥丁之祭弹出", gm._pending_choice == &"odin_sacrifice")
	var m1 := gm.get_state().memory.to_value()
	var r2: Dictionary = gm.resolve_choice(&"odin_sacrifice", &"b")
	_check("献名字成功", bool(r2.get("ok", false)))
	_check("献名字记忆+6", is_equal_approx(gm.get_state().memory.to_value() - m1, 6.0))
	_check("漂移注入+1", gm.get_state().drift_extra == 1.0)
	# 3) 记忆 35 + 林地民醒 → 诺恩过去/现在
	s.memory = BigNum.new(35.0)
	s.races["forestfolk"] = {"awakened": true, "population": 50.0}
	gm._process(1.0)
	_check("诺恩过去弹出", gm._pending_choice == &"norne_past")
	gm.resolve_choice(&"norne_past", &"a")
	_check("过去真相+2", gm.get_state().truth == 3)
	gm._process(1.0)
	_check("诺恩现在弹出", gm._pending_choice == &"norne_now")
	var r3: Dictionary = gm.resolve_choice(&"norne_now", &"a")
	_check("用灵魂救它成功", bool(r3.get("ok", false)))
	_check("河底-2", gm.get_state().soul_river == 98)
	_check("林地民+3", float(gm.get_state().races["forestfolk"]["population"]) == 53.0)
	# 4) 石裔醒 → 诺恩未来
	s.races["stoneborn"] = {"awakened": true, "population": 20.0}
	gm._process(1.0)
	_check("诺恩未来弹出", gm._pending_choice == &"norne_future")
	gm.resolve_choice(&"norne_future", &"a")
	_check("造船 flag", gm.get_state().choice_flags.has(&"ship_built"))
	# 5) 夺梦林地民 → 菟丝子（夺梦森林民 yield 1.2 记忆——差值断言吸收）
	gm.plunder_race(&"forestfolk")
	gm._process(1.0)
	_check("菟丝子弹出", gm._pending_choice == &"dodder")
	var m4 := gm.get_state().memory.to_value()
	var r4: Dictionary = gm.resolve_choice(&"dodder", &"b")
	_check("继续吸成功", bool(r4.get("ok", false)))
	_check("继续记忆+8", is_equal_approx(gm.get_state().memory.to_value() - m4, 8.0))
	# 6) 领悟 8 → 忒修斯（C 解锁）
	s.insight = 8
	gm._process(1.0)
	_check("忒修斯弹出", gm._pending_choice == &"theseus")
	var r5: Dictionary = gm.resolve_choice(&"theseus", &"c")
	_check("我记得我是成功", bool(r5.get("ok", false)))
	_check("领悟+2", gm.get_state().insight == 10)
	_check("忒修斯记忆 flag", gm.get_state().choice_flags.has(&"theseus_remembered"))
	# 7) 防重复：七卡全 done 后无新明选
	gm._process(1.0)
	gm._process(1.0)
	_check("无剩余明选", gm._pending_choice == &"")
	# 8) 存档往返 + 旧档回退
	var d := gm.get_state().to_dict()
	var back := GameState.from_dict(d)
	_check("存档往返 choices_done", back.choices_done.size() == gm.get_state().choices_done.size())
	_check("存档往返 truth", back.truth == gm.get_state().truth)
	var old := GameState.from_dict({"tick": 5})
	_check("旧档回退", old.choices_done.is_empty() and old.truth == 0 and old.drift_extra == 0.0)
	gm.free()
	print("E2E 结果: %d 检查, %d 失败" % [_checks, _fails])
	quit(1 if _fails > 0 else 0)
```

> ⚠️ **E2E 记忆断言用差值模式的原因**：人族 `produce_memory=true`，每 `_process(1.0)` tick 产 50×0.001=0.05 记忆（mem_eff 未设=1.0），林地民夺梦又 +1.2——精确绝对值断言必被 tick 产出污染。resolve 前后取差值（resolve 本身不跑 tick）最稳。若执行中发现其他族（石裔 craft_sap>0）影响 sap/信仰，同样用差值吸收，勿改回绝对值。
>
> ⚠️ E2E 步骤 3 依赖明选逐一出顺序：norne_past（35）在 norne_now 之前——但 two 都满足时数组序 past 先（JSON 序）。造森林folk 醒在 memory 35 同一帧，`_check("诺恩过去弹出")` 成立靠 JSON 数组序（norne_past 在 norne_now 前）。若实现/JSON 顺序不一致，以设计 §4.1 数组序为准修复断言。

- [ ] **Step 2: 运行 E2E**

Run: `godot --headless --path . -s res://tests/e2e_m5g.gd`
Expected: 全 PASS，退出码 0

- [ ] **Step 3: 全量回归 + 冒烟**

Run: 全量测试 + `godot --headless --path . --quit-after 5`
Expected: 全绿（基线 201 + 全部新增用例）+ 冒烟通过

- [ ] **Step 4: 删 E2E 脚本 + Commit**

```bash
Remove-Item tests/e2e_m5g.gd
git add -A
git commit -m "feat: M5g 明选引擎完整（E2E 全链路 PASS 后删临时脚本）"
```

- [ ] **Step 5: 文档同步**——CONTINUE.md（状态表 + 六·十三 M5g 完成记录）+ ROADMAP.md（步骤 5 明选引擎 ✅，注明五卡落地/⑥⑦待续）。要点：
- 内容：`features/choices/`（ChoiceLibrary+ChoiceActions+choices.json 五卡全文）+ GameState 五账本 + RaceManager 梦产系数 + DriftActions 漂移叠加 + GameManager 停顿点明选 + UI 弹层
- 验证：单测数（201 + N）/ E2E PASS / 冒烟
- 教训：JSON 文本外置工作流确立；明选=停顿点（pending 占用）实现；flag 命名约定落定

```bash
git add docs/world-tree/CONTINUE.md docs/world-tree/ROADMAP.md
git commit -m "docs: M5g 明选引擎完成记录 + 路线图同步"
```

---

## Self-Review（writing-plans 自检）

**1. Spec 覆盖：**
- §三 架构：ChoiceActions ✅（T3/T4）、ChoiceLibrary ✅（T2）、choices.json ✅（T2）、GameState 五账本 ✅（T1）、RaceManager/DriftActions 接入 ✅（T5）、GameManager 集成 ✅（T6）、UI 弹层 ✅（T7）
- §3.2 ChoiceLibrary 四函数 ✅（T2 测试+实现）
- §3.3 ChoiceActions 全部静态函数 + 触发条件键全集（races/memory/faith/growth/insight/relation/plundered/soul_revived/soul_river_lte）✅（T3 测试逐键覆盖）
- §3.4 effects 全键（memory/faith/growth_pct/faith_pct/relation/insight/truth/drift/memory_eff/soul/flags）✅（T4 测试逐键覆盖）；soul 操作表 ✅（norne_now a/b）
- §3.5 race_memory_eff 并入产出 ✅（T5：0.7→0.035 精确断言）
- §3.6 drift_extra 叠加 ✅（T5：tier 边界 + clamp）
- §3.7 GameManager 信号/入口/pending ✅（T6）；3.8 UI ✅（T7）；3.9 flag ✅（T4 JSON flags 写入）
- §四 五卡触发数值与后果 ✅（T2 JSON 照抄设计 4.2；T4 逐卡断言）
- §五 测试策略表格全部套件 ✅（T1-T7 对应；E2E ✅ T8）
- §六 决策 1-10：JSON 外置 ✅/范围 ✅/停顿点 ✅（T6 pending）/truth/drift_extra/race_memory_eff/choice_flags ✅（T1）/③拆 3 项 ✅（JSON 3 条目）/明选特耗 ✅（T4 soul 操作）/触发阈值对齐 ✅（T3）

**2. Placeholder 扫描：** 全部步骤含实际代码/JSON/命令；无 TBD/TODO。两处 ⚠️ 已注明修正语（T6 测试笔误、T6 信号第 4 参语义裁决）；一处 flag 命名偏离已注明（human_nightmare_protected 统一）。

**3. 类型一致性：**
- `choice_id: StringName` 前缀在 get_choice/available/resolve 全线一致 ✅
- `soul_river` int（soul_cost 减法用 maxi 防负）✅；`drift_extra` float（JSON 1.0 → float 1.0）✅
- 信号签名 T6 定义 / T6 测试 / T7 连接三处一致（StringName/StringName/String/String）✅
- `option_unlocked` 在 T3 实现 / T4 测试（resolve 内调用）/ T7 UI（灰显）三处一致 ✅
- JSON flag 名（T2）＝ T4 测试断言（choice_flags contains）＝ E2E（T8：ship_built/theseus_remembered）三处一致 ✅