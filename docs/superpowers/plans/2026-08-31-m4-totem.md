# 世界树 里程碑 4（图腾线：野民漂移探针）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将野民图腾线机制化——石壁画随记忆总量渐进浮现 5 幅（漂移探针读数），每幅可解读一次得领悟值 + 真相文本（吞噬者即我渐进）；领悟值入存档与 UI。

**Architecture:** 沿用既有模式：`features/memories/`（spec §15 新目录，领悟值之家）新增 `TotemLibrary`（const 数据表，5 幅）与 `TotemActions`（RefCounted 纯静态）；`GameState` 增 `totem_interpreted`/`insight`（沿 relics_found 防御模式）；`GameManager` 增 `interpret_totem` 入口 + `totem_interpreted` 信号；UI 纯文本（无美术），野民面板图腾区。

**Tech Stack:** Godot 4.7.1（mono）+ GDScript（typed）+ GdUnit4 6.2.1（headless）。

**Spec:** `docs/superpowers/specs/2026-08-31-world-tree-m4-totem-design.md`（本计划唯一权威）；主规格 `docs/superpowers/specs/2026-08-31-world-tree-design.md`（§5.4 野民/§13.6 领悟值/§15 架构）

## Global Constraints

- **测试命令**（GdUnit4 6.2.1，不是 `--run-tests`）：
  - 全量：`godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode`
  - 单文件：同命令加 `--add res://tests/unit/test_xxx.gd`；可加 `-c` 关 fail-fast 看全部失败
  - **新增 class_name 脚本后先跑** `godot --headless --path . --import`
- **`is_equal_approx` 双参签名** `(expected, approx)`——断言统一带容差 `, 1e-4`
- 资源一律 `BigNum`（禁裸 float 存经济资源）；typed GDScript
- 中文文件一律用 write/edit 工具（禁 PowerShell 默认编码）
- **文风铁律**：五幅浮现/解读文本**必须逐字复制**设计文档 §五（诗歌化·柔和六则定稿），不得重写
- 存档兼容：M2/M3 旧档缺 totem 字段回退默认，不得损坏
- git master；每任务一个 commit；工作目录 `E:\world tree`

---

### Task 1: TotemLibrary 数据模块（5 幅）

**Files:**
- Create: `features/memories/totem_library.gd`
- Test: `tests/unit/test_totem_library.gd`

**Interfaces:**
- Consumes: 无
- Produces: `class_name TotemLibrary extends RefCounted`：
  - `const TOTEMS: Array[Dictionary]`（5 幅：id/threshold/reveal_text/interpret_text）
  - `static func all_totems() -> Array[Dictionary]`
  - `static func get_totem(id: int) -> Dictionary`（找不到返回空字典）
  - `static func totem_count() -> int`

- [ ] **Step 1: 写失败测试**（test_totem_library.gd）

```gdscript
extends GdUnitTestSuite

func test_totem_count() -> void:
	assert_that(TotemLibrary.totem_count()).is_equal(5)

func test_get_totem_fields() -> void:
	var t := TotemLibrary.get_totem(1)
	assert_that(int(t.get("id", 0))).is_equal(1)
	assert_that(t.has("threshold")).is_true()
	assert_that(t.has("reveal_text")).is_true()
	assert_that(t.has("interpret_text")).is_true()

func test_get_missing_totem_returns_empty() -> void:
	assert_that(TotemLibrary.get_totem(99).is_empty()).is_true()

func test_all_totems_unique_ids() -> void:
	var ids: Array = []
	for t in TotemLibrary.all_totems():
		var id: int = int(t.get("id", 0))
		assert_that(ids.has(id)).is_false()
		ids.append(id)

func test_thresholds_ascending() -> void:
	var prev := -1.0
	for t in TotemLibrary.all_totems():
		var th := float(t.get("threshold", -1.0))
		assert_that(th).is_greater(prev)
		prev = th

func test_texts_nonempty() -> void:
	# 文风交付物防漂移：浮现/解读文本不得为空或过短
	for t in TotemLibrary.all_totems():
		assert_that(str(t.get("reveal_text", "")).length()).is_greater(5)
		assert_that(str(t.get("interpret_text", "")).length()).is_greater(5)
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless --path . --import` 后单文件测试
Expected: FAIL（无法解析 TotemLibrary）

- [ ] **Step 3: 实现 totem_library.gd**（文本逐字复制设计文档 §五）

```gdscript
class_name TotemLibrary
extends RefCounted

const TOTEMS: Array[Dictionary] = [
	{
		"id": 1,
		"threshold": 0.0,
		"reveal_text": "石壁上多了一幅画。画的是一棵树——树冠遮天，根须伸进河流。河在变浅。",
		"interpret_text": "它们在画你。它们从第一天就在画你——你才知道，那不是风景，是证据。",
	},
	{
		"id": 2,
		"threshold": 4.0,
		"reveal_text": "画里多了一行小人。跪在树前，黑黑的一排，像种子。",
		"interpret_text": "那是人。活过、哭过、把名字刻进石头的人。你仔细看——它们的脸，正对着你。",
	},
	{
		"id": 3,
		"threshold": 10.0,
		"reveal_text": "树的根须，缠着什么东西。像拥抱，又像收紧。",
		"interpret_text": "它们不画仇恨。它们只画事实。你吞下去的，不止是梦。",
	},
	{
		"id": 4,
		"threshold": 20.0,
		"reveal_text": "画旁多了一双眼睛。不眨，看着你，已经看了很久。",
		"interpret_text": "那是野民的眼睛。也是你看自己的眼睛。中间没有隔着河。",
	},
	{
		"id": 5,
		"threshold": 35.0,
		"reveal_text": "画的角落里，树的倒影里，站着一个人影。树与人影，是同一个。",
		"interpret_text": "你想起自己醒来时，手里那一点希望。它从来不是火种。是最后的证据——旧世界记得你，你忘了你也是它。",
	},
]

static func all_totems() -> Array[Dictionary]:
	return TOTEMS

static func get_totem(id: int) -> Dictionary:
	for t in TOTEMS:
		if int(t.get("id", 0)) == id:
			return t
	return {}

static func totem_count() -> int:
	return TOTEMS.size()
```

- [ ] **Step 4: 运行确认通过**

Run: 单文件测试
Expected: PASS（6 用例）

- [ ] **Step 5: Commit**

```bash
git add features/memories/ tests/unit/test_totem_library.gd tests/unit/test_totem_library.gd.uid
git commit -m "feat: TotemLibrary 数据模块（5 幅图腾浮现/解读文本）"
```

---

### Task 2: GameState 扩展（totem_interpreted/insight）

**Files:**
- Modify: `features/game/game_state.gd`
- Modify: `tests/unit/test_game_state.gd`

**Interfaces:**
- Consumes: 无新依赖
- Produces: `GameState.totem_interpreted: Array[int] = []`、`GameState.insight: int = 0`；`to_dict` 含 `"totem_interpreted"`/`"insight"`；`from_dict` 回退默认 + totem_interpreted 非数字过滤（沿 relics_found 模式，game_state.gd:68-73）

- [ ] **Step 1: 写失败测试**（test_game_state.gd 追加）

```gdscript
func test_totem_fields_roundtrip() -> void:
	var s := GameState.new()
	s.totem_interpreted.assign([1, 3])
	s.insight = 2
	var back := GameState.from_dict(s.to_dict())
	assert_that(back.totem_interpreted).contains(1)
	assert_that(back.totem_interpreted).contains(3)
	assert_that(back.insight).is_equal(2)

func test_totem_fields_missing_fallback() -> void:
	# M2/M3 旧档无 totem 字段——回退默认不损坏
	var back := GameState.from_dict({"tick": 5})
	assert_that(back.totem_interpreted).is_empty()
	assert_that(back.insight).is_equal(0)

func test_from_dict_filters_invalid_totem_ids() -> void:
	var back := GameState.from_dict({"totem_interpreted": [1, "x", {"a": 1}, 3.0]})
	assert_that(back.totem_interpreted.size()).is_equal(2)
	assert_that(back.totem_interpreted).contains(1)
	assert_that(back.totem_interpreted).contains(3)
```

- [ ] **Step 2: 运行确认失败**

Run: 单文件测试
Expected: FAIL（字段不存在）

- [ ] **Step 3: 扩展 game_state.gd**

```gdscript
# 新字段（races 附近）：
var totem_interpreted: Array[int] = []
var insight: int = 0

# to_dict() 中追加：
"totem_interpreted": totem_interpreted,
"insight": insight,

# from_dict() 中，relics_found 过滤后追加（同模式）：
var ti: Array = d.get("totem_interpreted", [])
var ti_cleaned: Array = []
for x in ti:
	if typeof(x) == TYPE_INT or typeof(x) == TYPE_FLOAT:
		ti_cleaned.append(int(x))
s.totem_interpreted.assign(ti_cleaned)
s.insight = int(d.get("insight", 0))
```

- [ ] **Step 4: 运行确认通过**

Run: 单文件测试
Expected: PASS（3 新用例 + 旧用例无回归）

- [ ] **Step 5: Commit**

```bash
git add features/game/game_state.gd tests/unit/test_game_state.gd
git commit -m "feat: GameState 扩展（totem_interpreted/insight + 存档回退）"
```

---

### Task 3: TotemActions 逻辑（visible_stage/can_interpret/interpret/next_interpretable）

**Files:**
- Create: `features/memories/totem_actions.gd`
- Test: `tests/unit/test_totem_actions.gd`

**Interfaces:**
- Consumes: `TotemLibrary`（T1）、`GameState`（memory/races/totem_interpreted/insight，T2）
- Produces: `class_name TotemActions extends RefCounted`：
  - `static func visible_stage(state: GameState) -> int`（当前浮现的最大幅 id；野民未唤醒返回 0）
  - `static func can_interpret(state: GameState, totem_id: int) -> bool`（幅已浮现且未解读）
  - `static func next_interpretable(state: GameState) -> int`（最新浮现且未解读的幅 id；无则 0——UI 解读目标）
  - `static func interpret(state: GameState, totem_id: int) -> Dictionary`（`{"ok": bool, "insight": int, "text": String}`；解读后 insight+1、标记已解读；不可解读 `{"ok": false}`）

- [ ] **Step 1: 写失败测试**（test_totem_actions.gd）

```gdscript
extends GdUnitTestSuite

func _awaken_wildfolk(state: GameState) -> void:
	state.races["wildfolk"] = {"awakened": true, "population": 80.0}

func test_visible_stage_zero_when_wildfolk_sleeping() -> void:
	var s := GameState.new()
	s.memory = BigNum.new(50.0)
	assert_that(TotemActions.visible_stage(s)).is_equal(0)

func test_visible_stage_bounds() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(3.99)
	assert_that(TotemActions.visible_stage(s)).is_equal(1)  # 幅 1 阈值 0
	s.memory = BigNum.new(4.0)
	assert_that(TotemActions.visible_stage(s)).is_equal(2)
	s.memory = BigNum.new(10.0)
	assert_that(TotemActions.visible_stage(s)).is_equal(3)
	s.memory = BigNum.new(35.0)
	assert_that(TotemActions.visible_stage(s)).is_equal(5)

func test_can_interpret_not_revealed() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(4.0)
	assert_that(TotemActions.can_interpret(s, 5)).is_false()  # 幅 5 未浮现

func test_can_interpret_revealed() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(4.0)
	assert_that(TotemActions.can_interpret(s, 2)).is_true()

func test_can_interpret_after_interpreted() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(35.0)
	s.totem_interpreted.assign([1, 2, 3, 4, 5])
	assert_that(TotemActions.can_interpret(s, 5)).is_false()

func test_interpret_gives_insight() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(4.0)
	var r := TotemActions.interpret(s, 2)
	assert_that(r.get("ok", false)).is_true()
	assert_that(int(r.get("insight", 0))).is_equal(1)
	assert_that(str(r.get("text", "")).length()).is_greater(5)
	assert_that(s.insight).is_equal(1)
	assert_that(s.totem_interpreted).contains(2)

func test_interpret_idempotent() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(4.0)
	TotemActions.interpret(s, 2)
	var again := TotemActions.interpret(s, 2)
	assert_that(again.get("ok", false)).is_false()
	assert_that(s.insight).is_equal(1)  # 不重复 +1

func test_interpret_not_revealed() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(4.0)
	assert_that(TotemActions.interpret(s, 5).get("ok", false)).is_false()

func test_next_interpretable() -> void:
	var s := GameState.new()
	_awaken_wildfolk(s)
	s.memory = BigNum.new(10.0)  # 幅 1-3 可见
	s.totem_interpreted.assign([1, 2])
	assert_that(TotemActions.next_interpretable(s)).is_equal(3)  # 最新未解读
	s.totem_interpreted.assign([1, 2, 3])
	assert_that(TotemActions.next_interpretable(s)).is_equal(0)  # 全解读完
```

- [ ] **Step 2: 运行确认失败**

Run: `godot --headless --path . --import` 后单文件测试
Expected: FAIL（无法解析 TotemActions）

- [ ] **Step 3: 实现 totem_actions.gd**

```gdscript
class_name TotemActions
extends RefCounted

static func _wildfolk_awakened(state: GameState) -> bool:
	return state.races.has(&"wildfolk") and bool(state.races[&"wildfolk"].get("awakened", false))

static func visible_stage(state: GameState) -> int:
	if not _wildfolk_awakened(state):
		return 0
	var stage := 0
	for t in TotemLibrary.all_totems():
		if state.memory.is_greater_or_equal(BigNum.new(float(t.get("threshold", 0.0)))):
			stage = maxi(stage, int(t.get("id", 0)))
	return stage

static func can_interpret(state: GameState, totem_id: int) -> bool:
	if totem_id <= 0 or totem_id > visible_stage(state):
		return false
	return not state.totem_interpreted.has(totem_id)

static func next_interpretable(state: GameState) -> int:
	for id in range(visible_stage(state), 0, -1):
		if not state.totem_interpreted.has(id):
			return id
	return 0

static func interpret(state: GameState, totem_id: int) -> Dictionary:
	if not can_interpret(state, totem_id):
		return {"ok": false}
	var totem := TotemLibrary.get_totem(totem_id)
	state.insight += 1
	state.totem_interpreted.append(totem_id)
	return {"ok": true, "insight": state.insight, "text": str(totem.get("interpret_text", ""))}
```

- [ ] **Step 4: 运行确认通过**

Run: 单文件测试
Expected: PASS（9 用例）

- [ ] **Step 5: Commit**

```bash
git add features/memories/ tests/unit/test_totem_actions.gd tests/unit/test_totem_actions.gd.uid
git commit -m "feat: TotemActions 逻辑（浮现阶段/解读/领悟/next_interpretable）"
```

---

### Task 4: GameManager 集成（interpret_totem 入口 + 信号）

**Files:**
- Modify: `autoloads/game_manager.gd`
- Modify: `tests/unit/test_game_manager.gd`

**Interfaces:**
- Consumes: `TotemActions`（T3）
- Produces: `signal totem_interpreted(totem_id: int, interpret_text: String)`；`func interpret_totem(totem_id: int) -> Dictionary`（成功时 emit 信号 + resources_changed）

- [ ] **Step 1: 写失败测试**（test_game_manager.gd 追加）

```gdscript
func test_interpret_totem_signal() -> void:
	# 野民唤醒 + 记忆达标 → 解读 → 信号 + 领悟 +1
	gm._state = GameState.new()
	gm._state.races["wildfolk"] = {"awakened": true, "population": 80.0}
	gm._state.memory = BigNum.new(4.0)
	var got := {"ok": false, "text": ""}
	gm.totem_interpreted.connect(func(id: int, text: String) -> void:
		got["ok"] = true
		got["text"] = text)
	var result: Dictionary = gm.interpret_totem(2)
	assert_that(result.get("ok", false)).is_true()
	assert_that(got["ok"]).is_true()
	assert_that(str(got["text"]).length()).is_greater(5)
	assert_that(gm.get_state().insight).is_equal(1)

func test_interpret_totem_blocked_no_signal() -> void:
	# 野民未醒 → 不可解读 → ok:false 且不发信号
	gm._state = GameState.new()
	gm._state.memory = BigNum.new(50.0)
	var got := {"ok": false}
	gm.totem_interpreted.connect(func(id: int, text: String) -> void: got["ok"] = true)
	var result: Dictionary = gm.interpret_totem(1)
	assert_that(result.get("ok", false)).is_false()
	assert_that(got["ok"]).is_false()
```

- [ ] **Step 2: 运行确认失败**

Run: 单文件测试
Expected: FAIL（interpret_totem 不存在）

- [ ] **Step 3: 修改 game_manager.gd**

```gdscript
# 信号区追加：
signal totem_interpreted(totem_id: int, interpret_text: String)

# 新增方法（explore_relic 之后）：
func interpret_totem(totem_id: int) -> Dictionary:
	var result := TotemActions.interpret(_state, totem_id)
	if result.get("ok", false):
		totem_interpreted.emit(totem_id, str(result.get("text", "")))
		resources_changed.emit()
	return result
```

- [ ] **Step 4: 运行确认通过**

Run: `godot --headless --path . --import` → 单文件测试 → 全量回归
Expected: 单文件 PASS（2 新用例）；全量 84 = 82 + 2 无回归

- [ ] **Step 5: Commit**

```bash
git add autoloads/game_manager.gd tests/unit/test_game_manager.gd
git commit -m "feat: GameManager 集成（interpret_totem 入口 + totem_interpreted 信号）"
```

---

### Task 5: UI 图腾区（纯文本，无美术）

**Files:**
- Modify: `features/ui/main.tscn`
- Modify: `features/ui/main.gd`

**Interfaces:**
- Consumes: `GameManager.interpret_totem`、`GameManager.get_state()`（T4）、`TotemActions.visible_stage/next_interpretable/can_interpret`（T3）、`TotemLibrary.get_totem`（T1）
- Produces: `%TotemPanel`（PanelContainer，含 `%TotemLabel`）、`%TotemInterpretButton`、`%InsightLabel`；main.gd `_refresh_totem()` + `_on_totem_pressed()` + `totem_interpreted` 连接

- [ ] **Step 1: 追加 main.tscn 节点**（RaceWildLabel 之后，VBox 末尾）

```
[node name="TotemPanel" type="PanelContainer" parent="VBox"]
unique_name_in_owner = true
visible = false
layout_mode = 2

[node name="TotemLabel" type="Label" parent="VBox/TotemPanel"]
unique_name_in_owner = true
layout_mode = 2
text = ""
autowrap_mode = 3

[node name="TotemInterpretButton" type="Button" parent="VBox"]
unique_name_in_owner = true
visible = false
layout_mode = 2
text = "解读图腾"

[node name="InsightLabel" type="Label" parent="VBox"]
unique_name_in_owner = true
visible = false
layout_mode = 2
text = "领悟：0"
```

- [ ] **Step 2: 扩展 main.gd**

```gdscript
# @onready 追加：
@onready var totem_panel: PanelContainer = %TotemPanel
@onready var totem_label: Label = %TotemLabel
@onready var totem_button: Button = %TotemInterpretButton
@onready var insight_label: Label = %InsightLabel

# _ready() 中（其他信号连接后）：
totem_button.pressed.connect(_on_totem_pressed)
GameManager.totem_interpreted.connect(_on_totem_interpreted)

# _refresh() 末尾追加：
_refresh_totem()

# 新增方法：
func _refresh_totem() -> void:
	var s := GameManager.get_state()
	var stage := TotemActions.visible_stage(s)
	if stage <= 0:
		totem_panel.visible = false
		totem_button.visible = false
		insight_label.visible = false
		return
	totem_panel.visible = true
	totem_button.visible = true
	insight_label.visible = true
	var totem := TotemLibrary.get_totem(stage)
	totem_label.text = "图腾·第 %d 幅\n%s" % [stage, str(totem.get("reveal_text", ""))]
	insight_label.text = "领悟：%d" % s.insight
	var next_id := TotemActions.next_interpretable(s)
	totem_button.disabled = next_id <= 0

func _on_totem_pressed() -> void:
	var s := GameManager.get_state()
	var next_id := TotemActions.next_interpretable(s)
	if next_id <= 0:
		return
	var result: Dictionary = GameManager.interpret_totem(next_id)
	if not result.get("ok", false):
		log_label.text = "画还看不清。再等等。"
	# 成功播报由 _on_totem_interpreted 处理

func _on_totem_interpreted(totem_id: int, interpret_text: String) -> void:
	race_event_label.text = interpret_text + "\n（领悟 +1）"
```

- [ ] **Step 3: 验证**

Run: `godot --headless --path . --import` → 全量测试（无回归）→ `godot --headless --path . --quit-after 5`（无 SCRIPT ERROR）

- [ ] **Step 4: Commit**

```bash
git add features/ui/main.tscn features/ui/main.gd
git commit -m "feat: UI 图腾区（浮现文本/解读按钮/领悟显示，纯文本）"
```

---

### Task 6: 集成验证与端到端

**Files:**
- Modify: 无（验证；临时脚本验证后删除）

- [ ] **Step 1: 全量测试**

Run: `godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode`
Expected: 84 全部 PASS、0 failures、退出码 0

- [ ] **Step 2: 端到端玩法验证（临时脚本，验证后删除）**

`_verify_m4.gd`（`extends SceneTree`，模拟：野民唤醒 → 记忆推进 5 幅浮现 → 解读 5 次领悟 5 → 存档往返 → M3 旧档回退 → 输出 `M4 E2E VERIFY PASSED` 或逐项 FAIL）：

```gdscript
extends SceneTree

func _init() -> void:
	var failures: Array[String] = []
	# 1. 野民未醒 → stage 0
	var s := GameState.new()
	s.memory = BigNum.new(50.0)
	if TotemActions.visible_stage(s) != 0:
		failures.append("stage not zero when sleeping")
	# 2. 唤醒 + 记忆推进 → 5 幅浮现
	s.races["wildfolk"] = {"awakened": true, "population": 80.0}
	s.memory = BigNum.new(35.0)
	if TotemActions.visible_stage(s) != 5:
		failures.append("stage not 5 at memory 35")
	# 3. 解读 5 次 → 领悟 5
	for id in [1, 2, 3, 4, 5]:
		var r := TotemActions.interpret(s, id)
		if not r.get("ok", false):
			failures.append("interpret %d failed" % id)
	if s.insight != 5:
		failures.append("insight != 5")
	# 4. 幂等：再解读不重复
	if TotemActions.interpret(s, 5).get("ok", false):
		failures.append("interpret not idempotent")
	# 5. 存档往返
	var back := GameState.from_dict(s.to_dict())
	if back.insight != 5 or back.totem_interpreted.size() != 5:
		failures.append("save roundtrip totem")
	# 6. M3 旧档回退
	var legacy := GameState.from_dict({"races": {"wildfolk": {"awakened": true, "population": 80.0}}})
	if legacy.insight != 0 or not legacy.totem_interpreted.is_empty():
		failures.append("legacy fallback")
	if failures.is_empty():
		print("M4 E2E VERIFY PASSED")
		quit(0)
	else:
		print("M4 E2E FAILED: ", failures)
		quit(1)
```

Run: `godot --headless --path . -s res://_verify_m4.gd`
Expected: 输出 `M4 E2E VERIFY PASSED`、退出码 0

- [ ] **Step 3: 清理 + 提交**

删除 `_verify_m4.gd`；`git status` 确认工作区干净；如有修复则提交。

---

## Self-Review 记录（写完计划时自查）

- **设计文档覆盖**：§3.1 TotemLibrary ✓（T1）；§3.2 TotemActions ✓（T3，含设计 UI 需要的 next_interpretable 细化）；§3.3 GameState+存档 ✓（T2）；§3.4 GameManager ✓（T4）；§3.5 UI ✓（T5）；§4 阈值 ✓（T1 数据 + T3 visible_stage）；§五 文本 ✓（T1 逐字）；§6 测试矩阵 ✓（T1-T6）。
- **占位符扫描**：无 TBD/TODO；所有测试与实现代码完整给出。
- **类型一致性**：`visible_stage -> int`、`can_interpret(state, id) -> bool`、`interpret -> Dictionary`、`next_interpretable -> int` 在 T3/T4/T5/T6 引用一致；`totem_interpreted: Array[int]`、`insight: int` 在 T2/T3/T4/T5 一致；`totem_interpreted(totem_id: int, interpret_text: String)` 信号在 T4/T5 签名一致。
- **边界核对**：visible_stage 幅 1 threshold 0 → 野民唤醒即 stage≥1（T3 test）；can_interpret 用 `totem_id > visible_stage` 挡未浮现（幅 id 1-5 与 TOTEMS 顺序一致）；interpret 幂等（已解读挡）；野民未醒所有接口返回"不可"（T3/T4 测试覆盖）。
- **UI 细节**：解读目标 = `next_interpretable`（最新浮现未解读幅），按钮 disabled 逻辑 `next_id <= 0`；野民未醒图腾区隐藏（visible=false）。
- **数值**：阈值 4/10/20/35 与设计 §4.1 一致；T6 E2E 用记忆 35 覆盖全部 5 幅。
- **已知行为变更（有意）**：无——图腾线是纯新增系统，不影响既有 82 测试语义。
- **迁移安全**：M2/M3 旧档缺 totem 字段回退默认（T2 测试 + T6 E2E 第 6 项）。
